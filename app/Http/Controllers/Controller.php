<?php

namespace App\Http\Controllers;

/**
 * Kelas dasar (base controller) yang diwarisi semua controller aplikasi.
 * Sengaja dibiarkan kosong; menjadi titik pusat bila nanti perlu perilaku
 * bersama untuk seluruh controller (mis. trait otorisasi/validasi).
 */
abstract class Controller {}
